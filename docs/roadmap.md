# Driving Trainer — Technical Roadmap

## M0 — Project Foundation

Status: Completed

Goals:

* Git repository created
* Go installed
* Go API running
* basic project structure created
* PostgreSQL + PostGIS running in Docker
* database accessible through DBeaver
* `POST /api/v1/routes/analyze` implemented
* endpoint validation implemented
* mocked route analysis returned
* `AGENTS.md` created

Definition of Done:

The backend can be executed locally and the first API endpoint can be validated successfully.

---

# M1 — Real Routing with Valhalla

Status: Completed

Goal:

Replace mocked route information with a real route calculated by Valhalla.

Tasks:

* run Valhalla locally
* prepare regional OpenStreetMap data
* configure Valhalla
* test Valhalla `/route` directly
* create internal Router abstraction
* create Valhalla HTTP client
* connect route service to Router
* return real distance
* return real duration
* return route geometry/polyline
* handle Valhalla errors
* create integration tests using mocks/test server

Completed notes:

* Valhalla runs locally through Docker Compose.
* The initial regional dataset is `sul-latest.osm.pbf`.
* Direct Valhalla `/status` and `/route` validation succeeded.
* `POST /api/v1/routes/analyze` now returns route data calculated by Valhalla.
* `route.polyline` contains Valhalla polyline6 geometry.
* Difficulty scores and route events intentionally remain placeholders for later milestones.

Do not implement yet:

* database persistence
* difficulty calculation
* elevation analysis
* Firebase
* Android

Definition of Done:

Given valid origin and destination coordinates:

```text
POST /api/v1/routes/analyze
```

returns a real route generated by Valhalla including:

* distance
* duration
* geometry

No mocked route data remains.

---

# M2 — Route Persistence

Status: Completed

Goal:

Persist real route information using PostgreSQL/PostGIS.

Tasks:

* create initial migrations
* configure pgx
* configure sqlc
* create Route repository
* store route geometry as PostGIS LINESTRING
* persist route metadata
* retrieve stored route
* add indexes

Initial entities:

```text
Route
```

Completed notes:

* PostgreSQL access is configured through `DATABASE_URL`.
* pgx is used for the database connection and sqlc generates typed query access.
* `POST /api/v1/routes/analyze` persists Valhalla routes after decoding `route.polyline` as polyline6.
* The official persisted geometry is `GEOGRAPHY(LINESTRING, 4326)`.
* The original Valhalla polyline6 is stored as auxiliary route metadata.
* `GET /api/v1/routes/{id}` returns basic persisted route data.
* Difficulty placeholders and route events are not persisted in this milestone.

Definition of Done:

A route generated through Valhalla can be stored and retrieved from PostgreSQL with valid geographic geometry.

---

# M3 — Route Segmentation

Status: Completed

Goal:

Convert a route into meaningful segments for later difficulty analysis.

Tasks:

* inspect Valhalla edge/attribute output
* define segmentation rules
* create RouteSegment domain model
* identify road changes
* identify road-class changes
* preserve segment order
* persist segment geometry
* persist available road metadata

Completed notes:

* Valhalla `trace_attributes` edges are converted into ordered RouteSegments.
* Adjacent edges with the same road class, road name, road use and speed limit are grouped into one segment.
* Segment geometry is sliced from Valhalla shape indexes.
* Segment distance, estimated duration, available street names, road class, road use and speed limit are persisted.
* `GET /api/v1/routes/{id}` returns persisted segments ordered by sequence.
* `POST /api/v1/routes/analyze` returns the segments created for the route.

Future notes:

* Additional road metadata such as surface and lane count can be added from `trace_attributes` when needed by later difficulty/event milestones.

Definition of Done:

A route can be represented as an ordered collection of meaningful RouteSegments.

---

# M4 — Elevation and Hill Detection

Status: Completed

Goal:

Detect elevation changes and hills.

Tasks:

* configure elevation DEM
* integrate elevation data
* calculate elevation profile
* calculate average incline
* estimate maximum incline
* detect uphill sections
* detect downhill sections
* create HILL event
* create STEEP_HILL event

Completed notes:

* Local Valhalla is configured to build elevation data.
* The Valhalla client enriches calculated routes with `/height` elevation samples when DEM data is available.
* Route segments are enriched with elevation start/end, average incline and maximum incline.
* `HILL` and `STEEP_HILL` events are detected deterministically from segment slope.
* Segment slope metrics and route events are persisted in PostgreSQL/PostGIS.
* `POST /api/v1/routes/analyze` and `GET /api/v1/routes/{id}` return persisted hill events.

Definition of Done:

A route containing relevant elevation changes produces structured hill events and slope metrics.

---

# M5 — Curve Analysis

Status: Completed

Goal:

Identify driving-relevant curves.

Tasks:

* decode route geometry
* calculate heading changes
* estimate curve severity
* distinguish maneuver turns from continuous road curves
* detect curve sequences
* create CURVE event
* create SHARP_CURVE event
* create CURVE_SEQUENCE event

Completed notes:

* Route segment geometry is analyzed with deterministic heading-change rules.
* Continuous same-direction heading changes produce `CURVE` or `SHARP_CURVE` events.
* Isolated turns at segment boundaries are filtered to reduce maneuver-turn false positives.
* Nearby curve events are grouped into `CURVE_SEQUENCE` events.
* Curve events are persisted through the existing `route_events` table and returned by `POST /api/v1/routes/analyze` and `GET /api/v1/routes/{id}`.

Definition of Done:

The system identifies and classifies meaningful curves along a route.

---

# M6 — Intersection and Road Events

Status: Completed

Goal:

Identify important driving situations from road/intersection data.

Initial events:

* STOP
* TRAFFIC_LIGHT
* ROUNDABOUT
* INTERSECTION
* COMPLEX_INTERSECTION
* HIGHWAY_ENTRY
* HIGHWAY_EXIT

Completed notes:

* Added a deterministic road event analyzer for `INTERSECTION`, `COMPLEX_INTERSECTION`, `ROUNDABOUT`, `HIGHWAY_ENTRY` and `HIGHWAY_EXIT`.
* Valhalla `trace_attributes` requests documented road/intersection attributes and maps them into domain-level road event hints.
* Segment transitions are used as a conservative fallback for relevant intersections and highway entry/exit events when detailed node hints are incomplete.
* Nearby roundabout and highway transition events are deduplicated to avoid one real-world situation appearing once per Valhalla edge.
* `POST /api/v1/routes/analyze` persists and returns road events alongside existing hill and curve events.
* `GET /api/v1/routes/{id}` returns persisted road events.
* Validated with local Valhalla routes for `INTERSECTION`, `COMPLEX_INTERSECTION`, `ROUNDABOUT`, `HIGHWAY_ENTRY` and `HIGHWAY_EXIT`.

Known limitation:

* `STOP` and `TRAFFIC_LIGHT` are supported by the domain analyzer when source data is present, but the current Valhalla `trace_attributes` integration did not expose those attributes in local validation.
* Future support for reliable `STOP` and `TRAFFIC_LIGHT` detection may require Valhalla tiles, `/locate`, custom Valhalla attributes, or direct OSM/PostGIS enrichment.

Definition of Done:

The route analysis contains structured events corresponding to relevant real-world driving situations.

---

# M7 — Compound Event Detection

Goal:

Combine nearby/simple events into meaningful driving situations.

Examples:

```text
HILL + STOP
→ HILL_STOP

HIGH_SPEED + MERGE
→ HIGHWAY_ENTRY
```

Tasks:

* define spatial/event association rules
* detect HILL_STOP
* detect complex intersections
* detect relevant event combinations

Definition of Done:

The system can distinguish a simple hill from situations such as a mandatory stop on an incline.

Status:

Implemented in the Go route domain layer.

* `HILL_STOP` is detected when a `HILL` or `STEEP_HILL` event is near a `STOP` event along the route.
* `HILL_STOP` is also detected when a route starts or ends on a `HILL` or `STEEP_HILL`, using the route boundary as a stopped-vehicle context.
* `COMPLEX_INTERSECTION` can also be inferred from a nearby `INTERSECTION` plus `STOP`, `TRAFFIC_LIGHT` or `ROUNDABOUT` when Valhalla/source hints did not already classify it as complex.
* Compound events are deterministic, persisted through the existing `route_events` table and returned by `POST /api/v1/routes/analyze` and `GET /api/v1/routes/{id}`.

Notes:

* Reliable `HILL_STOP` coverage still depends on the routing/source data exposing STOP events.
* Difficulty scoring remains placeholder-level until M8.

---

# M8 — Difficulty Engine V1

Goal:

Calculate deterministic route difficulty.

Inputs:

* segments
* hills
* curves
* intersections
* speed characteristics
* compound events
* event density
* event diversity

Outputs:

```text
overallDifficulty
averageDifficulty
peakDifficulty
complexityScore
categoryScores
```

Definition of Done:

The same route and engine version always produce the same score and category breakdown.

The engine must have strong unit-test coverage.

---

# M9 — Android Foundation

Goal:

Create the first Android application.

Technology:

* Kotlin
* Jetpack Compose
* Hilt
* Coroutines / Flow

Tasks:

* create project
* configure modules
* configure networking
* implement backend health check
* implement basic design system
* create initial navigation

Definition of Done:

Android can communicate with the local Go backend.

---

# M10 — Map and Route Builder

Goal:

Allow users to create routes visually.

Tasks:

* map screen
* current location
* select origin
* select destination
* add waypoints
* submit route for analysis
* render route
* display route analysis

Definition of Done:

The user can create a route and see its real analyzed characteristics in the Android application.

---

# M11 — Navigation

Goal:

Navigate a selected training route.

Planned provider:

* HERE SDK Navigate

Tasks:

* navigation abstraction
* HERE integration
* turn-by-turn guidance
* route progress
* voice guidance
* basic offline behavior

Definition of Done:

A route generated by the backend can be followed through the Android navigation experience.

---

# M12 — Users and Authentication

Goal:

Introduce persistent users.

Technology:

* Firebase Auth

Tasks:

* Android authentication
* backend Firebase token validation
* internal users table
* map Firebase UID to internal user

Definition of Done:

Authenticated users can access user-specific backend resources.

---

# M13 — Training Sessions

Goal:

Track the execution of a route as a training session.

Entities:

```text
TrainingSession
TrainingFeedback
```

Flow:

```text
Start
Pause
Resume
Complete / Abort
Feedback
```

Definition of Done:

A user can complete a route and retain the training history.

---

# M14 — Progression

Goal:

Add experience and gamification.

Features:

* XP
* level
* experience by category
* total training distance
* achievements

No reward may depend on:

* maximum speed
* shortest completion time
* aggressive driving

Definition of Done:

Completed training sessions update the user's experience and progression.

---

# M15 — Training Engine

Goal:

Recommend or generate routes based on the driver's experience.

Inputs:

* user experience
* goal
* available time
* desired difficulty zone
* current location

Difficulty zones:

```text
COMFORT
EVOLUTION
CHALLENGE
```

Definition of Done:

The system can produce candidate routes and recommend one that matches the driver's training goal and current experience.

---

# M16 — Android Auto

Goal:

Expose safe navigation functionality through Android Auto.

Priorities:

* minimal interaction
* clear instructions
* safe templates
* no active gamification during driving

Definition of Done:

Supported training routes can be navigated using Android Auto within platform safety restrictions.

---

# M17 — Advanced Telemetry

Future milestone.

Potential features:

* acceleration analysis
* braking events
* driving smoothness
* OBD2 integration
* personalized route recommendations

This milestone must only begin after the core training experience has been validated.
